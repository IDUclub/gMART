# IDE
.idea
.vscode

# Py Cache
__pycache__/

# Network certificates
*.crt
DECFILE

# ENV files
.env
.env.idu_mcp.development
.env.idu_mcp.develop
.env.idu_mcp.dev
.env.idu_mcp.prod
.env.idu_mcp.production
.env.agents.development
.env.agents.develop
.env.agents.dev
.env.agents.prod
.env.agents.production

# Data Files
/data

# Logs
*.log

# mongo
/mongo

# Frontend build
frontend/node_modules/
frontend/dist/

#Agents
AGENTS.md

# Skills
/graphify-out

# Benchmark data and outputs (large, local-only: gold set, run results, reports).
# The harness and eval code under benchmarks/ IS tracked on the experiment branch,
# so only the data directories are ignored here — not the whole folder.
benchmarks/data/
benchmarks/out/

# local scenario cache (SCENARIO_CACHE_DIR default)
runtime/

# Benchmark-run credentials (local only)
env/.env.benchmark.local
