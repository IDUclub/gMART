# Публикация изменений SDK и harness

Ветка gMART: `feat/orchestrator-sdk-harness`. Она содержит изменения
этой задачи начиная с исходных `5f00d03` и `b0825de` (прежняя база `bc46e66`), включая локальный
стек, целевую оркестрацию, бюджет/контекст, продолжение, доставку артефактов,
функциональный и индустриальный harness и исправления до `e736740`.
Дополнительно включены первоначальный отчёт dev-интеграции и контроль этапов
`tests/integration/industrial/progress.py` с регрессионными тестами.

## Связанные репозитории

| Репозиторий | Ветка публикации | Коммиты задачи | Изменение |
| --- | --- | --- | --- |
| ObjectEffectsAPI | `fix/orchestrator-sdk-integration` | `24b802b` | Конкретная ошибка отсутствующего норматива с областью расчёта, сохранение доступных результатов |
| NormGraph | `fix/orchestrator-sdk-integration` | `a6d39cd`, `6a5cb54` | Повтор неполного извлечения и различение физических зданий и услуг при построении CheckPlan |

ChatStorage и IDU_DVD использовались из существующих checkout. Конфигурация
их подключения для этой задачи включена в compose-файлы gMART; отдельных
изменений исходного кода этих сервисов в данной задаче не было.

## Проверенное состояние

- gMART: **1186 Linux unit-тестов** прошли, включая 12 проверок учёта этапов
  (`output/industrial/stability-linux-unit-17.log`).
- Последний развёрнутый код оркестратора: `e736740`; локальный сервисный preflight
  прошёл **13/13** проверок источников, DVD, канонического compliance, расчётов
  и геометрии (`stability-candidate-preflight-01`).
- Последняя завершённая индустриальная серия на прежнем коде `687ff8b`:
  **1/15 диалогов**, **16/30 реплик** прошли структурные проверки. Fingerprint
  сборки не менялся. Исправления после этой серии не считаются живой приёмкой.
- Следующая попытка `stability-04` на `e736740` остановилась на preflight:
  серверы vLLM и эмбеддингов недоступны, DNS `a.dgx` не разрешается.
  **Ни одного аналитического запроса не начато.**

Контроль этапов засчитывает сценарий только после трёх независимых успешных
двухрепликовых диалогов, включая judge, структурные проверки и replay.
Новый этап требует повторного успеха всех ранее принятых сценариев на той же
сборке. Промежуточные 3/5 сценариев не подменяют строгий итоговый критерий 15/15.

Сырые живые трассы, геоданные и локальные файлы с секретами остаются вне Git.
Методика, воспроизводимый harness и подробные диагностические отчёты включены
в ветку. Публикация ветки не означает завершение приёмки или deployment в dev.

## Rebase на актуальный dev

Ветка gMART перенесена на `origin/dev` = `77d01e8`. Приведённые выше SHA
обозначают исходную историю до rebase. Конфликт в `pipeline_state.py` разрешён
с сохранением обеих функций: хранения документного диалога и аналитического
контекста. Новые подписчики документных потоков теперь передают владельца
в защищённое чтение журнала, включая проверку курсора при reconnect.
Проверка чужого владельца сохранена; регрессия покрывает публичный и
авторизованный потоки и предварительную проверку REST-роута.

После объединения gMART: **1238 Linux unit-тестов прошли**
(`rebase-gmart-unit-02.log`). IDU_DVD `feat/full-reparse` перенесена на
`origin/dev` = `84de73b`; её исходный коммит уже включён в dev и не дублируется.
В чистой копии без локальных dev-настроек прошли **857 unit-тестов**
(`rebase-dvd-unit-04.log`). Локальные диагностические файлы IDU_DVD сохранены.
Живые серии после rebase не запускались.
