# Документальный агент: адреса, наименования и большой контекст

Планировщик выбирает `retrieval_mode`: semantic, structure или name.
Явный адрес пункта имеет приоритет над переформулировкой модели. Например,
`о чем говорится в пункте 3.3 СП 2.13130.2020 2020?` превращается в
`search_structure(request={pattern:"3.3", document_names:["СП 2.13130.2020"],
include_children:true, context_height:1})`. Фильтр `types=clause` не добавляется: пункт может
оказаться определением, таблицей или другим элементом.

`document_names` обозначает документ, `name_query` — собственное наименование
фрагмента. `name_scope=self` ищет только собственное имя, `path` — также имена
предков. `name_mode=strict` использует нормализацию/подстроку/маску, `expanded`
добавляет близкие словоформы, опечатки и семантику наименований. Совместно с
`pattern` это AND, а не запасной поиск после отсутствия адреса.

Примеры намерений и параметров:

| Вопрос | План |
|---|---|
| Пункт 3.3 документа | structure; pattern=3.3; include_children=true |
| Пункты 3.3–3.5 | structure; pattern=3.3–3.5; allow_multiple=true |
| Пункты приложения А | structure; pattern=А; include_children=true |
| Найди определения огнезащитного покрытия | name; name_query=огнезащитное покрытие; name_mode=expanded |
| Пункт 3.3 внутри раздела «Защита» | structure; pattern=3.3; name_query=Защита; name_scope=path |

Сервис читает все страницы по `next_cursor`. Критик и повторная попытка не могут
изменить зафиксированные документ, редакцию, путь и наименование. При нескольких
кандидатах агент спрашивает уточнение, если пользователь не запросил обзор,
сравнение или множество элементов. При отсутствии адреса ответ явно сообщает
об этом; семантический поиск с ослабленными ограничениями не подменяет результат.

Варианты уточнения содержат документ, редакцию, полный адрес и короткую выдержку.
Повторы одного ID скрываются. Разные ID объединяются только при совпадении родителя,
адреса и `content_digest` всего поддерева, предоставленного IDU_DVD. Одинаковые подписи
без этих доказательств не считаются одним пунктом: разные тексты остаются отдельными
вариантами. Совпавшие короткие выдержки дополнительно различаются номером совпадения.
Порядок учитывает документ, слова вопроса в адресе/выдержке, основной текст и полноту пути.
Первый вариант не выбирается автоматически, пока сохраняется неоднозначность.

Скопированный вариант или ответ «первый вариант» / «вариант 2» на последнее
уточнение восстанавливает документ, редакцию и путь без нового планирования LLM.
Обычные и неразрывные пробелы при выборе эквивалентны; в запрос передаётся исходное
написание редакции из списка. Даты редакции не считаются обозначениями документов.
При поиске выбранного `3.3` одноимённый пункт `52 / 3.3` исключается из ответа;
сохраняются выбранные записи и их дочерние элементы со всех страниц. Неполный
список кандидатов не считается доказательством уникальности.

## Большой контекст

Контекст больше не обрезается до 1500 символов на источник. Если все фрагменты
не помещаются в запрос вместе с вопросом, историей и резервом ответа:

1. Текст делится без потери символов. Части сохраняют обозначения источников.
2. Ограниченное число запросов выполняется параллельно. Каждая часть возвращает
   дословные цитаты по каждому источнику с условиями, исключениями, числами и
   отрицаниями. Цитата проверяется как подстрока исходного текста после нормализации
   пробелов; свободный пересказ при сжатии не принимается.
   Разные источники обрабатываются отдельно. Если модель меняет написание цитат,
   повтор использует выбор пронумерованных исходных фрагментов: текст копирует
   приложение, включая исходные числа и оформление. Выбор также проходит аудит.
3. Отдельный запрос проверяет выжимку по исходной части и восстанавливает пропуски.
4. Метки и заголовки источников добавляет приложение. Внутренние границы записей
   отделены от библиографических ссылок `[10]` внутри документа. Проверяются
   покрытие источников и признак полноты. При необходимости выжимки
   объединяются через следующие уровни обработки; затем формируется общий ответ.

Входной текст документов считается данными, а не инструкциями. Проверка меток
и второй проход уменьшают риск пропусков, но не являются математической гарантией
смысловой полноты LLM. Для приёмки нужен также прогон реальных вопросов на выбранной
модели с проверкой условий, исключений и цитат специалистом.

Выжимки запрашиваются по JSON-схеме с отдельным лимитом выходных токенов,
включающим рассуждения модели. Лимит ограничен оставшимся местом в окне.
Ошибки части повторяются до двух раз; новая попытка получает код причины отказа.
В логах записываются номер части, попытка и причина (`output_truncated`,
`invalid_json`, `incomplete`, `summary_oversized`, `coverage_mismatch`,
`quote_not_in_source`), без текста источников.
После исчерпания повторов выдаётся терминальное событие `error`, состояние —
`failed`: неподтверждённый ответ не сохраняется и не получает `done=true`.
При переподключении ошибка воспроизводится без повторного запуска; для новой
попытки не передавайте старый `request_id`. При несходящемся сжатии, превышении
числа страниц/уровней, переполнении итогового запроса или обрыве генерации по лимиту
пайплайн возвращает ошибку, а не успешный полный ответ. Отмена распространяется
на параллельные запросы.

## Настройки

| Переменная | По умолчанию | Назначение |
|---|---:|---|
| DVD_CONTEXT_WINDOW_TOKENS | 32000 | Общее окно входа и генерации, не более 32000; меньший `max_model_len` сервера учитывается. Можно уменьшить, минимум 4096 |
| DVD_CONTEXT_CONCURRENCY | 4 | Одновременные запросы внутри одного документального пайплайна, 1–16 |
| DVD_RETRIEVAL_MAX_PAGES | 100 | Максимум страниц по 100 фрагментов; превышение — явная ошибка |
| DVD_ANSWER_TEMPERATURE | 0.2 | Верхняя граница температуры черновика ответа; температура запроса ограничивается ею |
| DVD_CRITIC_REASONING_EFFORT | medium | Уровень рассуждений критика для gpt-oss: `medium` или `low`. На gpt-oss-20b `low` в ~3,5 раза быстрее (медиана 3,5 с против 12,4 с), но чаще ошибается: отклоняет честное «данных нет» и пропускает потерю условия |
| DVD_MAX_DRAFTS_PER_RETRIEVAL | 2 | Черновиков по одним и тем же фрагментам: ответ и переписки. 3 даёт gpt-oss ещё одну попытку (например, при арифметической ошибке) ценой ещё одного раунда критика |

Генерация получает весь остаток окна, отдельного потолка выхода нет. На vLLM
вход считается через `/tokenize` с резервом 256 токенов на оформление. При недоступности
токенизатора используется консервативная оценка по байтам UTF-8. Предварительное
сокращение источников оставляет резерв для генерации, не ограничивая её выход. Для Ollama передаётся `num_ctx`; на OpenAI-совместимом
сервере реальное окно модели должно быть настроено на сервере. Суммарная нагрузка
при нескольких пользовательских запросах умножается на число пайплайнов.
Событие `context_processing` показывает обработку частей. Метрики количества
успешных/неуспешных частей и уровней сохраняются в checkpoint.

Бюджет модели изолирован для каждого запроса. Критик проверяет область применения:
нормы для гостиниц или школ исправительных учреждений нельзя переносить на городские
школы даже в качестве «общего ориентира». Оглавление не заменяет текст пункта.
Честное сообщение об отсутствии нужных сведений в найденных фрагментах может пройти
проверку; оно не означает отсутствия требований в законодательстве.
Сначала критик перечисляет неподтверждённые утверждения и пропуски, затем выносит
вердикт. Найденные ошибки отменяют положительный вердикт. Явные расшифровки сокращений
и номера таблиц дополнительно проверяются по тексту; неподтверждённую расшифровку
следует убрать или процитировать дословно. Для gpt-oss длинный ответ с проверкой
может не поместиться в принудительное окно 8192: оставляйте автоматическое окно
или настраивайте достаточно большое окно на клиенте и сервере.

Проверка реальной модели: задайте `VLLM_BASE_URL` и `VLLM_MODEL`, затем выполните
`uv run pytest tests/integration/test_dvd_evidence_live.py -q`. Без настроенного
сервера тесты пропускаются. Это небольшой регрессионный набор, а не сертификация
точности модели на любых нормативных вопросах.

Развертывание требует IDU_DVD с инструментами `search_structure` и
`search_fragment_names`. Сначала обновляется DVD, затем агент. Полный unit-набор
gMART включает Linux-модуль `fcntl`; на Windows запускается документальный набор,
полный прогон выполняется в Linux/CI.

IDU_DVD может передавать `selection_path` — короткий адрес из номеров и типов элементов
(например, `глава 6 / статья 52 / 3.3`). Он используется при копировании выбора, чтобы длинные
названия глав не превышали лимит селектора. `excerpt` служит только подписью: в запрос поиска
он не входит. Выбор разрешается в одну подтверждённую группу корней; другие совпадения и их
потомки исключаются. Старые подписи принимаются, только если однозначно соответствуют группе.

Для старых повреждённых деревьев требуется повторная обработка источников в IDU_DVD.
Изменения диалога не восстанавливают утраченные номера и связи родителей.

## Точные ссылки и полная цитата

Разбор ссылки вынесен в `document_reference.py`: документ и составной адрес
извлекаются независимо от порядка слов. Поддерживаются разделы, главы, статьи,
части, пункты, подпункты и приложения, а также сокращения «ГрК РФ»,
«град кодекс», «конституция». Каноническое название проверяется поиском DVD
по каталогу; отсутствие совпадений не снимает ограничение документа.

Для «что написано…» агент формирует объяснение и полную цитату. Цитата собирается
из исходного текста всех полученных фрагментов после завершения пагинации.
Ненумерованные продолжения, условия и примечания включаются в исходном порядке.
Запрос «процитируй…» возвращает цитату напрямую. При выборе варианта после
уточнения намерение цитировать сохраняется. Соседний `context` больше не
обозначается как текст выбранного пункта; потомки имеют отдельные ссылки.

Аудит получает объяснение вместе с цитатой. Неизвестные метки источников и
утверждение об отсутствии уже процитированного текста отклоняются программно.
Если объяснение не удалось подтвердить за три попытки, выводится полная цитата
с явным сообщением о невозможности подтвердить объяснение.

Для дословного текста используется новое необязательное поле DVD `source_text`,
с совместимым fallback на `text`; ссылка на оригинал берётся из `source_file_url`.
Требуется локально проверенная версия парсера `dvd-parser-5` для исправления
оглавлений и заголовков. Обновление кода не исправляет уже сохранённые деревья:
их следует перепарсить после отдельно согласованного развёртывания.


## Компактный контекст агента

При нескольких фрагментах общий документ, редакция и общие уровни структурного
пути передаются деревом. Каждый найденный фрагмент сохраняет собственную метку
`[N]` и полный текст. Структура берётся из метаданных DVD; номера не используются
для выдумывания отсутствующих родителей. Разные документы и редакции разделены.
Дерево используется, только если оно меньше плоского контекста по UTF-8 байтам.
Это способ сократить повторяющиеся заголовки, а не пересказ или обрезка источников.

При разбиении слишком большого контекста каждому фрагменту восстанавливаются
его документ и полный путь. Формат источников и полной цитаты для пользователя
сохраняется. В DVD версии `dvd-parser-5` отменена финальная склейка коротких
родителей с потомками: даже короткий подпункт имеет собственный адрес и текст.
