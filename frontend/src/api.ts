import type {Chat,ChatSummary,Settings,StreamEvent} from "./types";
export async function request<T>(base:string,path:string,token:string,init:RequestInit={}):Promise<T>{const response=await fetch(new URL(path,base.replace(/\/+$/,"")+"/"),{...init,headers:{Authorization:`Bearer ${token}`,"Content-Type":"application/json",...init.headers}});if(!response.ok){const body=await response.json().catch(()=>null);throw new Error(body?.detail?.message??body?.detail??body?.message??`Ошибка API: ${response.status}`)}return response.json()}
export async function readSse(url:URL,token:string,signal:AbortSignal,onEvent:(event:StreamEvent)=>void){const response=await fetch(url,{headers:{Authorization:`Bearer ${token}`,Accept:"text/event-stream"},signal});if(!response.ok)throw new Error(`Не удалось открыть поток: ${response.status} ${await response.text()}`);const reader=response.body?.getReader();if(!reader)throw new Error("Пустой SSE-поток");const decoder=new TextDecoder();let buffer="";while(true){const {done,value}=await reader.read();if(done)break;buffer+=decoder.decode(value,{stream:true}).replace(/\r\n/g,"\n");const blocks=buffer.split("\n\n");buffer=blocks.pop()??"";for(const block of blocks){const data=block.split("\n").filter(x=>x.startsWith("data:")).map(x=>x.slice(5).trimStart()).join("\n");if(data)onEvent(JSON.parse(data))}}}
export async function getChats(settings:Settings,token:string,scenario?:string){const q=new URLSearchParams({limit:"60",offset:"0"});if(scenario)q.set("scenario_id",scenario);return request<{items:ChatSummary[]}>(settings.chatStorageUrl,`/api/v1/chat_history/chats?${q}`,token)}
export const getChat=(s:Settings,t:string,id:string)=>request<Chat>(s.chatStorageUrl,`/api/v1/chat_history/${id}`,t);
export const deleteChat=(s:Settings,t:string,id:string)=>request(s.chatStorageUrl,`/api/v1/chat_history/${id}`,t,{method:"DELETE"});
export const getModels=(s:Settings,t:string)=>request<string[]>(s.agentsUrl,"/llm/available_models",t);
